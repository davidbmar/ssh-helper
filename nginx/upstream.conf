# SSH Helper Upstream Configuration
# This file is managed in the ssh-helper repository
# Deploy to: /etc/nginx/conf.d/system-upstreams/ssh-helper.conf

upstream ssh_terminal {
    server 127.0.0.1:8080;
}
